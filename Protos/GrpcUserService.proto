syntax = "proto3";

service GrpcUserService {
    rpc UserExisting (UserIdRequest) returns (UserExistingResponse);
}

message UserIdRequest {
    string guid = 1;
}

message UserExistingResponse {
    bool success = 1;
}